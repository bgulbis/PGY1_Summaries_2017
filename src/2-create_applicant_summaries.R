# create applicant summaries

library(tidyverse)
library(stringr)
library(ReporteRs)

dirr::get_rds("data/tidy")

save_dir <- "report/summaries"

# for (i in 1:nrow(data_applicants)) {
for (i in 1:5) {

}
